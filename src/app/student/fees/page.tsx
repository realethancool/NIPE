'use client';
import {useEffect,useState} from 'react';
export default function FeesPage(){const[f,setF]=useState<any>(null);useEffect(()=>{fetch('/api/student/fees').then(x=>x.json()).then(d=>setF(d.fees))},[]);return <main className="min-h-screen bg-cream p-4 md:p-8"><div className="mx-auto max-w-5xl"><h1 className="text-3xl font-bold">Fees</h1>{!f?<div className="mt-6 rounded-2xl bg-white p-8">No fee account found.</div>:<><div className="mt-6 grid gap-4 sm:grid-cols-3"><div className="rounded-2xl bg-white p-5">Total<h2 className="text-2xl font-bold">₹{f.totalAmount}</h2></div><div className="rounded-2xl bg-white p-5">Paid<h2 className="text-2xl font-bold">₹{f.paidAmount}</h2></div><div className="rounded-2xl bg-white p-5">Pending<h2 className="text-2xl font-bold">₹{f.pendingAmount}</h2></div></div><div className="mt-4 rounded-2xl bg-white p-5">Due date: {f.dueDate?new Date(f.dueDate).toLocaleDateString():'Not set'}</div></>}</div></main>}
