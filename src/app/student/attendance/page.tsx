'use client';
import {useEffect,useMemo,useState} from 'react';
export default function AttendancePage(){const[a,setA]=useState<any[]>([]);useEffect(()=>{fetch('/api/student/attendance').then(r=>r.json()).then(d=>setA(d.attendance||[]))},[]);const byDate=useMemo(()=>Object.fromEntries(a.map(x=>[new Date(x.date).toISOString().slice(0,10),x])),[a]);return <main className="min-h-screen bg-cream p-4 md:p-8"><div className="mx-auto max-w-5xl"><h1 className="text-3xl font-bold">Attendance</h1><p className="text-muted mt-1">Green = present, red = absent.</p><div className="mt-6 grid grid-cols-7 gap-2">{Array.from({length:31},(_,i)=>{const d=String(i+1).padStart(2,'0');const date=`${new Date().getFullYear()}-${String(new Date().getMonth()+1).padStart(2,'0')}-${d}`;const x=byDate[date];return <div key={d} className={`min-h-20 rounded-xl p-3 border ${x?.status==='PRESENT'?'bg-green-100 border-green-300':x?.status==='ABSENT'?'bg-red-100 border-red-300':'bg-white border-line'}`}><b>{i+1}</b>{x&&<p className="mt-2 text-xs">{x.subject.code}<br/>{x.status}</p>}</div>})}</div></div></main>}
