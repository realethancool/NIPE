import {NextRequest,NextResponse} from 'next/server';import {PrismaClient} from '@prisma/client';import {getFacultySession} from '../../../../../lib/faculty-auth';const prisma=new PrismaClient();export async function GET(req:NextRequest){const s=getFacultySession();if(!s)return NextResponse.json({error:'Unauthorized'},{status:401});const subjectId=new URL(req.url).searchParams.get('subjectId');if(!subjectId)return NextResponse.json({error:'subjectId is required'},{status:400});try{const a=await prisma.facultySubject.findUnique({where:{facultyId_subjectId:{facultyId:s.facultyId,subjectId}},include:{subject:true}});if(!a)return NextResponse.json({error:'You are not assigned to this subject'},{status:403});const students=await prisma.student.findMany({where:{status:'ACTIVE',departmentId:a.subject.departmentId,semesterId:a.subject.semesterId},select:{id:true,enrollmentNumber:true,name:true},orderBy:{name:'asc'}});return NextResponse.json({students})}catch{return NextResponse.json({error:'ERP database migration is required'},{status:503})}}
