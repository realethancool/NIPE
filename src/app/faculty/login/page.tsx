'use client';
import {FormEvent,useState} from 'react';import {useRouter} from 'next/navigation';
export default function FacultyLogin(){const r=useRouter();const[e,setE]=useState('');const[p,setP]=useState('');const[err,setErr]=useState('');const[loading,setL]=useState(false);async function go(x:FormEvent){x.preventDefault();setL(true);setErr('');try{const z=await fetch('/api/faculty/login',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({employeeNumber:e,password:p})});const d=await z.json();if(!z.ok)throw Error(d.error);r.push('/faculty/dashboard')}catch(x:any){setErr(x.message||'Login failed')}finally{setL(false)}}return <main className="min-h-screen flex items-center justify-center bg-cream p-4"><form onSubmit={go} className="w-full max-w-md rounded-2xl bg-white p-8 shadow-xl"><h1 className="text-3xl font-bold">Faculty Login</h1>{err&&<p className="mt-4 rounded bg-red-50 p-3 text-red-700">{err}</p>}<input required placeholder="Employee number" value={e} onChange={x=>setE(x.target.value)} className="mt-6 w-full rounded-lg border p-3"/><input required type="password" placeholder="Password" value={p} onChange={x=>setP(x.target.value)} className="mt-3 w-full rounded-lg border p-3"/><button disabled={loading} className="mt-5 w-full rounded-lg bg-gold p-3 font-semibold">{loading?'Signing in…':'Login'}</button></form></main>}
